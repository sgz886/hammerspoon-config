-- local utils = require("modules.utils")

-- ⌥⇧⌘T：把选中文字发送到"ChatBox", 然后翻译
-- doesn't need, use Wdgesture instead:
-- hs -c "require('modules.utils').sendSelectionToApp('Chatbox')"



-- hs.hotkey.bind({"ctrl", "shift", "cmd"}, "L", function()
--   utils.sendSelectionToApp("Chatbox")
--   print("run workflow")
-- end)

-- print("✅ workflows 已加载")
